# 更新日志

本文件记录插件每个版本的变更，面向使用者，按时间倒序排列。

> 提示：本插件管理的是静态博客（Firefly）。**站点配置、主题配置等修改后，都需要重新构建并部署博客才会在网站上生效**（管理页「状态」页提供一键按钮，或对 AI 说"构建并部署"）。

## [1.10.0] - 2026-08-09

### 新增

- **分类与标签查询工具**：新增 `list_blog_categories` 和 `list_blog_tags` 两个 LLM 工具，AI 可直接查看博客所有分类和标签（按文章数排序）
- **文章搜索增强**：搜索支持正则表达式，自动退化为文本匹配，无需精确匹配
- **文章备份**：编辑和删除文章前自动备份到 `backups/` 目录（每个文件最多保留 5 份），备份数据存放在 AstrBot 数据目录（`StarTools.get_data_dir()`）
- **构建取消与日志持久化**：构建过程中可调用 `cancel_build()` 取消；构建日志自动保存到 `build_logs/` 目录（保留最近 20 条），支持异常中断后回溯
- **投稿缓存上限**：投稿数据超过 500 条时自动淘汰最旧的已拒绝投稿，防止缓存无限增长
- **投稿参数扩展**：`/博客投稿` 指令现支持 `tags` 和 `category` 参数

### 修复

- **修复文章置顶字段丢失**：`PostInfo` 模型新增 `pinned` 字段，`BlogManager` 构造文章列表时正确传递置顶状态
- **修复重命名文章时的事务安全**：重命名操作改为先写新文件、再删旧文件，失败时自动回滚
- **修复备份目录位置**：备份文件从插件目录迁移到 AstrBot 数据目录（`StarTools.get_data_dir()/backups/`），插件更新不再丢失备份

### 安全

- **shlex.quote 全面加固**：所有用户输入拼接到 shell 命令处均使用 `shlex.quote()`，涉及 `remote_blog_root`、`RemoteExecutor.run` cwd、31 处命令拼接
- **WebUI 路径安全**：`_resolve_src_path` 增加 symlink `realpath` 校验，防止通过符号链接越界访问 `src/` 目录外文件
- **文件上传白名单**：上传接口新增 `UPLOAD_ALLOWED_EXTS` 类型限制，拒绝可执行文件和危险格式
- **XSS 检测**：文件编辑器检测 `<script>` 标签，返回 `xss_warning` 前端提示
- **文件删除确认**：删除操作需要 `confirm=true` 参数，防止误删
- **写入文件必须存在**：文件编辑接口拒绝创建新文件（需通过上传或新建接口）
- **构建任务上限**：同时排队的构建任务限制为 20 个，超限自动清理最旧的
- **构建超时保护**：异步构建任务添加 3600 秒超时，防止无限挂起
- **远端状态路径脱敏**：远程构建模式下状态接口不再暴露服务器实际路径，显示为 `[remote]`
- **部署失败日志**：远端部署失败时记录详细错误日志

### 改进

- **配置文件列表排序**：核心配置文件排在前面，按字母序排列
- **文件列表返回总数**：API 响应新增 `total` 字段，支持前端分页

## [1.9.0] - 2026-08-09

### 管理页改版

- 「对外展示」更名为「站点配置」并大幅扩展，现在可在同一页面管理博客几乎所有对外内容；页签顺序调整为：状态 / 站点配置 / 文件管理 / 主题配置 / 插件配置
- **友链**：条目式行编辑（标题、链接、头像、描述、标签、权重、启用），可任意添加/删除友链
- **站点信息**：网站标题、副标题、网址、描述、关键词；同时管理友链申请页的展示信息与申请邮箱
- **公告**：标题、内容、是否可关闭、链接
- **音乐**：播放来源、平台、歌单 ID、导航栏入口、歌词
- **留言板**：评论系统（Twikoo / Waline / Giscus / Disqus / Artalk / 关闭）与服务地址
- **打赏**（新增）：标题、描述、使用说明与展示开关；打赏方式（名称、图标、收款码、外链、描述、启用）与打赏者名单（名称、金额、日期）均支持行式增删
- **相册**（新增）：列宽 + 相册条目（ID、名称、描述、地点、日期、标签、访问密码）行式增删；图片放到博客 `public/gallery/<相册ID>/` 目录即可
- **关于我**：头像、名字、签名、邮箱，社交链接行式增删
- **RSS**：订阅地址由站点信息自动生成并联动展示

### 新增

- **插件配置页**：管理页新增「插件配置」页签，全部插件配置以表单展示（开关/下拉/数字/文本），保存即时生效；SSH 密码等敏感字段不回显、留空不修改

## [1.8.9] - 2026-08-09

### 新增

- **主题配置可视化**：管理页新增「主题配置」页签，将博客全部主题配置（站点 / 外观 / 布局导航 / 互动内容 / 站务 / 关于与社交，共 6 组）可视化编辑——开关、数字、文本、枚举下拉、列表、对象数组表格编辑；由代码生成的配置（如导航栏主体）自动识别为只读，引导使用「文件管理」编辑
- 保存主题配置前自动备份原文件；保存后需重新构建部署生效（配合状态页一键按钮）
- **状态页一键构建与部署**：新增「仅构建」与「构建并部署」按钮，自动完成 环境检查 → 依赖安装（如需）→ 构建 → 部署，页面实时展示进度

## [1.8.8] - 2026-08-09

### 新增

- 「站点信息」独立页：编辑网站基本信息（标题、副标题、网址、描述、关键词）与友链申请页展示的站点信息（含申请邮箱）
- 友链申请信息（friends.mdx）纳入「对外展示」管理

## [1.8.7] - 2026-08-08

### 修复

- **修复一键部署脚本在"博客目录与部署目录相同"时误删博客源码的严重问题**

### 优化

- 一键部署脚本网络稳定性大幅提升：国内镜像一键加速（`USE_CN_MIRROR=1`）、下载/克隆/远程操作全部带超时、非交互环境自动跳过确认，不再"莫名其妙卡住"

## [1.8.6] - 2026-08-08

### 新增

- 管理页**随机壁纸背景**：自动从博客壁纸目录随机选取，配暗色遮罩保证可读性
- 未检测到已部署的博客时，页面顶部提示引导使用部署脚本

## [1.8.5] - 2026-08-08

### 修复

- 移除点击文件/按钮后的蓝色焦点框（键盘导航仍保留焦点指示）

## [1.8.4] - 2026-08-08

### 改进

- 界面动效与质感：按钮按压动画、文件夹展开丝滑过渡、页签切换淡入、卡片悬停浮起

## [1.8.3] - 2026-08-08

### 变更

- 移除「站点配置」页签（与「对外展示」功能重叠），配置文件统一从「文件管理」打开编辑

### 修复

- 图片文件（png/jpg/gif/webp 等）打不开的问题，改为内嵌预览显示

## [1.8.2] - 2026-08-08

### 改进

- 「文件管理」视觉引导：文件类型彩色徽标、目录层级加粗与竖线导轨、README/文档文件蓝色高亮

## [1.8.1] - 2026-08-08

### 修复

- 「对外展示」页内容加载白屏的问题
- 「关于我」（profileConfig.ts）无法在「对外展示」中编辑的问题

## [1.8.0] - 2026-08-08

### 新增

- **「对外展示」页**上线：集中管理友链、社交链接、页脚等对外展示内容，通用表格编辑（默认含 friendsConfig / socialConfig / footerConfig，可在插件配置中追加）
- 管理页配色改为插件 logo 的荧光绿-青绿系

## [1.7.0] - 2026-08-08

### 改进（触控与新手易用性）

- 文件树整行可点击（手机端加大点击区）
- `src/config/` 下的 README/文档文件高亮展示，点击直接跳转查看
- 有未保存修改时离开页面会弹窗确认，防止误触丢失
- 手机端编辑区字号与工具栏优化

## [1.6.1] - 2026-08-08

### 修复

- **修复管理页面所有操作都显示"未知错误"的严重问题**

## [1.6.0] - 2026-08-08

### 新增

- 「站点配置」页签：结构化编辑博客配置文件（siteConfig、footerConfig、sponsorConfig、commentConfig 等），嵌套对象/数组均可编辑；代码动态值自动只读保留

## [1.5.0] - 2026-08-08

### 新增

- **Dashboard 管理页面**上线（AstrBot 面板 → 插件详情 → 页面），含三个页签：
  - **博客状态**：部署模式、博客目录、构建状态、文章数量、内存状态
  - **文件管理**：树形浏览/编辑/新建/删除/上传博客 `src/` 目录文件（支持远程模式）
  - **友链管理**：图形化编辑友链列表
- 未检测到博客目录时可在页面手动填写，或一键重新自动检测

## [1.4.1] - 2026-08-08

### 修复

- **修复远程构建（remote_build）模式下部署时误删博客仓库的严重缺陷**
- 修复 KaTeX 进阶语法文档中矩阵/化学方程式示例的转义说明

### 优化

- 构建并发锁与内存限制配置正式生效
- 远程操作响应速度提升；权限判定优先复用 AstrBot 框架管理员体系

## [1.4.0] - 2026-06-27

### 新增

- **投稿系统**：任何人可投稿、管理员审核后发布（`/博客投稿`、`/我的投稿`），投稿数据持久化保存
- **AI 初审**：投稿时自动调用大模型评估（评分、优缺点、改进建议），管理员可查看初审结果后决定过审/打回
- **进阶语法**：GitHub 卡片、Admonitions、Spoiler、图片画廊、Expressive Code、Mermaid、PlantUML、KaTeX，8 个子开关，各附优劣说明
- **帮助菜单**：`/博客帮助` 展示全部指令、权限与用法
- SSH 安全加固：主机密钥严格校验、密码经临时文件传递（不暴露在进程列表）、命令注入防护、日志脱敏

## [1.3.x] - 2026-06-23

### 新增

- 投稿功能：提交/审核/发布完整流程与相关指令
- 管理员列表配置 `admin_users`；`allow_only_owner`、`owner_user_id` 权限控制

### 修复与优化

- 兼容不同版本 AstrBot 框架
- 投稿数据自动持久化，插件重启不丢失

## [1.2.0] - 2026-06-23

### 新增

- 构建前内存检查：内存不足自动跳过构建并提示（阈值可配置）
- 构建内存上限与并发控制配置
- 内存状态 / 构建资源 / 构建配置查看指令

## [1.1.0] - 2026-06-22

### 新增

- 一键部署脚本支持自动配置 Nginx/Apache、HTTPS（HTTP 自动跳转 HTTPS）、自定义域名
- 构建前资源监控与告警

### 修复

- 修复文章日期被写成字符串的问题（改为正确日期类型，符合 Astro 要求）

## [1.0.0] - 2026-06-22

### 首发

- **文章管理**：创建、删除、列出、查看、更新、搜索（完整支持 YAML Front-matter）
- **构建部署**：环境检查 → 依赖安装 → 构建 → 部署，支持三种模式（本地构建远程部署 / 远程构建 / 单机部署），SSH 自动重连，传输优先 rsync、失败回退 scp
- **一键部署脚本** deploy.sh / deploy.ps1：自动安装依赖、克隆博客仓库（支持镜像加速）、配置 Web 服务器
