{
  "type": "object",
  "properties": {
    "connection_type": {
      "type": "string",
      "title": "连接类型",
      "description": "选择博客服务器位置",
      "enum": ["local", "remote"],
      "default": "local"
    },
    "local_blog_root": {
      "type": "string",
      "title": "本地博客根目录",
      "description": "当连接类型为 local 时，指定本机博客的根目录",
      "default": "/var/www/firefly"
    },
    "server_ip": {
      "type": "string",
      "title": "远程服务器 IP",
      "description": "当连接类型为 remote 时，运行 Firefly 博客的服务器 IP 地址"
    },
    "server_port": {
      "type": "integer",
      "title": "SSH 端口",
      "description": "远程服务器的 SSH 端口",
      "default": 22
    },
    "username": {
      "type": "string",
      "title": "SSH 用户名",
      "description": "远程服务器的登录用户名"
    },
    "auth_type": {
      "type": "string",
      "title": "认证方式",
      "description": "远程服务器 SSH 认证方式",
      "enum": ["key", "password"],
      "default": "key"
    },
    "private_key_path": {
      "type": "string",
      "title": "SSH 私钥路径",
      "description": "密钥认证时必填，本地私钥文件的绝对路径"
    },
    "password": {
      "type": "string",
      "title": "SSH 密码",
      "description": "密码认证时必填，SSH 登录密码",
      "format": "password"
    },
    "remote_blog_root": {
      "type": "string",
      "title": "远程博客根目录",
      "description": "远程服务器上 Firefly 博客的根目录",
      "default": "/var/www/firefly"
    }
  },
  "required": ["connection_type"]
}